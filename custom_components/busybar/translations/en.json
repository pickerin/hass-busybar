{
  "config": {
    "flow_title": "{name}",
    "step": {
      "user": {
        "title": "Connect to BUSY Bar",
        "data": {
          "host": "Host (IP address)",
          "api_key": "API key (only if HTTP access mode is set to 'key')"
        }
      },
      "zeroconf_confirm": {
        "title": "Add BUSY Bar",
        "description": "Add {name} to Home Assistant?"
      }
    },
    "error": {
      "cannot_connect": "Cannot connect to the device",
      "invalid_auth": "Invalid API key"
    },
    "abort": {
      "already_configured": "This BUSY Bar is already configured",
      "cannot_connect": "Cannot connect to the device"
    }
  },
  "entity": {
    "sensor": {
      "busy_status": {
        "name": "Busy status",
        "state": {
          "idle": "Idle",
          "busy": "Busy",
          "timer": "Timer",
          "interval": "Interval"
        }
      }
    },
    "switch": {
      "busy": { "name": "Busy" }
    },
    "number": {
      "brightness": { "name": "Brightness" },
      "volume": { "name": "Volume" }
    }
  },
  "services": {
    "draw_text": {
      "name": "Draw text",
      "description": "Draw text on the BUSY Bar display.",
      "fields": {
        "device_id": { "name": "Device", "description": "Target BUSY Bar (optional if only one is configured)." },
        "text": { "name": "Text", "description": "Text to display." },
        "display": { "name": "Display", "description": "Front (outward) or back (user-facing) display." },
        "font": { "name": "Font", "description": "Font name." },
        "color": { "name": "Color", "description": "Hex color, e.g. #FF0000." },
        "x": { "name": "X", "description": "Horizontal position." },
        "y": { "name": "Y", "description": "Vertical position." },
        "align": { "name": "Align", "description": "Anchor alignment." },
        "timeout_ms": { "name": "Timeout", "description": "Remove the text after this many milliseconds." },
        "scroll_rate": { "name": "Scroll rate", "description": "Scroll speed for long text." }
      }
    },
    "clear_display": {
      "name": "Clear display",
      "description": "Remove content drawn via the API.",
      "fields": {
        "device_id": { "name": "Device", "description": "Target BUSY Bar (optional if only one is configured)." }
      }
    },
    "play_audio": {
      "name": "Play audio",
      "description": "Play an uploaded (path) or built-in (stock_path) sound.",
      "fields": {
        "device_id": { "name": "Device", "description": "Target BUSY Bar (optional if only one is configured)." },
        "path": { "name": "Path", "description": "Path to an uploaded audio asset." },
        "stock_path": { "name": "Stock path", "description": "Name of a built-in sound." }
      }
    },
    "stop_audio": {
      "name": "Stop audio",
      "description": "Stop audio playback.",
      "fields": {
        "device_id": { "name": "Device", "description": "Target BUSY Bar (optional if only one is configured)." }
      }
    }
  }
}
